def solution(numbers):
    sum = 0
    for i in numbers:
        sum += i
    
    avg = sum / len(numbers)
    # avg_list = avg.split(".")
    # if avg_list[1] % 6 == 0
    # answer = 0
    return avg